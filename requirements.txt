# 음식 분류 + 양(그램) 추정 + 칼로리 파이프라인 런타임 의존성
# Python 3.10~3.13 권장
#
# [설치]
#   py -3.11 -m venv .venv311
#   .\.venv311\Scripts\python -m pip install --upgrade pip
#   .\.venv311\Scripts\python -m pip install -r requirements.txt
#
# ※ 분류·양(그램)·칼로리 모두 Gemini가 담당하므로 GEMINI_API_KEY 가 필요합니다
#   (환경변수 또는 프로젝트 루트 .gemini_key 파일). 로컬 YOLO/ResNet 모델은 제거되어
#   torch/torchvision 은 더 이상 필요 없습니다.

google-genai>=0.3
opencv-python>=4.8
numpy>=1.24
pillow>=9
pandas>=1.5
openpyxl>=3.1
PyYAML>=6.0
tqdm>=4.6
matplotlib>=3.5
